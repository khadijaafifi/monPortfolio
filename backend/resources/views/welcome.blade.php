{{-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Portfolio</title>
</head>
<body>
    {{-- <h1>{{ $profile->name }}</h1>
    <h2>{{ $profile->title }}</h2>
    <p>{{ $profile->bio }}</p>
    <p><em>{{ $profile->philosophy }}</em></p>
    <img src="{{ $profile->image }}" alt="Photo de {{ $profile->name }}" width="200"> --}}

    {{-- <h2>Projets</h2>
    <ul>
        @foreach($projects as $project)
            <li>
                <strong>{{ $project->title }}</strong> - {{ $project->description }}<br>
                Tech: {{ implode(', ', json_decode($project->technologies)) }}<br>
                <a href="{{ $project->url }}" target="_blank">Voir le projet</a>
            </li>
        @endforeach
    </ul>

    <h2>Compétences</h2>
    <ul>
        @foreach($skills as $skill)
            <li>{{ $skill->name }} - {{ $skill->level }}%</li>
        @endforeach
    </ul>
</body>
</html> --}} 
